
import React, { useEffect, useState } from "react";
import { Moon, Sun } from "lucide-react";
import { Toggle } from "@/components/ui/toggle";
import { cn } from "@/lib/utils";

// Componente removido pois não precisamos mais da funcionalidade de tema escuro
const ThemeToggle = () => {
  // Este componente está vazio, pois estamos removendo a funcionalidade de tema escuro
  return null;
};

export default ThemeToggle;
